namespace PasswordBruteForcer;

/// <summary>
/// Application entry point. The functionality is filled in across the following task commits:
/// hashing, password generation, the brute-force generator/validator, the multithreaded engine,
/// performance logging and finally the Windows Forms GUI.
/// </summary>
static class Program
{
    [STAThread]
    static void Main()
    {
        // GUI and headless benchmark modes are added in later versions (see git history / README).
    }
}
